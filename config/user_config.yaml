# User-specific configuration
# WARNING: This file contains sensitive information. Do not commit to version control.

integrations:
  email:
    username: "your.email@gmail.com"  # Replace with your email
    password: "your-app-specific-password"  # Replace with your app password

# User configuration for Jarvis

# Voice settings
voice:
  elevenlabs_api_key: ""  # ElevenLabs API key

# Security settings
security:
  encryption_key: ""  # Add your encryption key here if needed 
